const playAnimation = (currentScene) => {
  switch (currentScene) {
    case 0: {
      alert(currentScene);
      break;
    }
    case 1:
      alert(currentScene);
      break;

    case 2:
      alert(currentScene);
      break;

    case 3:
      alert(currentScene);
      break;

    default:
      break;
  }
};

export default playAnimation;
